import React from "react";
import { Text, ScrollView } from "react-native";

function TrackMedicine() {
    return (
        <ScrollView>
            <Text>You are in Medicine Tracker</Text>
        </ScrollView>
    );
}

export default TrackMedicine;
