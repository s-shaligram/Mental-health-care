import React from "react";
import { Text, ScrollView } from "react-native";

function ScreenA() {
    return (
        <ScrollView>
            <Text>You are in Screen A</Text>
        </ScrollView>
    );
}

export default ScreenA;
